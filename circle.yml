machine:
 timezone:
  Asia/Tokyo

test:
 override:
  - echo "test"

deployment:
 production:
  branch: master 
  commands: 
   - aws s3 sync . s3://melonwithoutphp2/ --delete
 yml:
  branch: yml
  commands:
   - aws s3 sync . s3://melonwithoutphp3/ --delete

